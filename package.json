{
  "name": "sls-apigw-schema-plugin-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "types": "json2ts -i 'models/**/*.json' > 'types.ts'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sls-apigw-schema-plugin": "^0.0.1"
  },
  "dependencies": {
    "json-schema-to-typescript": "^11.0.2"
  }
}
